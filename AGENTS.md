# factory — Agent Context

**Read first:** `../../../AGENTS.md` — Manav's workspace brain (identity, rules, preferences). This project inherits all workspace rules.

## What this project is

Manav's fork of `owainlewis/factory`, used to evolve the evaluated upstream
agent control plane into a personal software factory with explicit, pinned,
auditable plugins that can later be adapted to approved work environments.

## Upstream relationship

- `origin` is `manavhirey/factory`; `upstream` is `owainlewis/factory`.
- Keep upstream changes reviewable and preserve compatibility unless an
  accepted fork design explicitly says otherwise.
- Never place credentials, runtime state, product worktrees, or private
  experiment logs in this repository.

## Conventions

- Design docs → `docs/specs/YYYY-MM-DD-<topic>-design.md`
- Implementation plans → `docs/plans/YYYY-MM-DD-<topic>.md`
- Current status lives in the workspace dashboard: `../../../context/projects.md`

